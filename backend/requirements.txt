# PharmGuard backend — runs on the Pi (production) AND on dev-mac (BACKEND_HEADLESS=1).
# Install via:
#   pip install -r requirements.txt --extra-index-url https://www.piwheels.org/simple
# Pi-only deps are gated by `platform_machine == 'aarch64'` so a Mac dev venv
# silently ignores them. Mediapipe needs cp <= 3.12; on Pi install with
# PHARMGUARD_PYTHON=python3.12 (see scripts/install.sh).

# ── HTTP server / Pydantic / auth ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
httpx>=0.27.0
websockets>=12.0

# ── Data plane ──
supabase>=2.0.0
requests>=2.31.0,<3.0.0

# ── ML / vision ──
ultralytics>=8.0.0,<9.0.0
opencv-python-headless>=4.8.0,<5.0.0
mediapipe>=0.10.9,<0.11.0
numpy>=1.24,<2.0
google-generativeai>=0.8.0

# ── Pi-only (skip on Mac dev venv) ──
picamera2>=0.3.12,<0.4.0; platform_machine == 'aarch64'
rpi-lgpio>=0.6; platform_machine == 'aarch64'
